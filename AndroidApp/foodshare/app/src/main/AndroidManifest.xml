<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.foodshare">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-feature android:name="android.hardware.camera" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.example.android.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <activity android:name="com.example.foodshare.SettingActivity"></activity>
        <activity android:name="com.example.foodshare.LaunchScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.example.foodshare.MapsActivity_selectAddress"
            android:label="@string/title_activity_maps_select_address" />
        <activity
            android:name="com.example.foodshare.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name="com.example.foodshare.UserEditActivity" />
        <activity android:name="com.example.foodshare.SignInActivity" />
        <activity
            android:name="com.example.foodshare.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name="com.example.foodshare.NewPostActivity" />
        <activity android:name="com.example.foodshare.PostDetailActivity" />
        <activity android:name="com.example.foodshare.GoogleSignInActivity" />
        <activity android:name="com.example.foodshare.SignupActivity" />
        <activity android:name="com.example.foodshare.MyPostActivity" />
        <activity android:name="com.example.foodshare.ChatActivity" />
        <activity android:name="com.example.foodshare.SendOfferActivity" />
        <activity android:name="com.example.foodshare.MyPostOfferActivity" />
        <activity android:name="com.example.foodshare.PostRateActivity" />
        <activity android:name="com.example.foodshare.OfferRateActivity" />

    </application>

</manifest>